{
  "name": "@qasignal/mcp-server",
  "version": "0.1.0",
  "description": "MCP Server for QASignal",
  "bin": {
    "qasignal": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format cjs --dts",
    "dev": "tsup src/index.ts --format cjs --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@qasignal/core": "*"
  }
}
